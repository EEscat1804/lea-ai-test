# CPU-only PyTorch wheels — much smaller than the default CUDA build, and
# Render's standard plans don't have a GPU to use anyway. If this ever moves
# to a GPU-backed host, drop this line and set SENSEVOICE_DEVICE=cuda:0.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.*
uvicorn[standard]==0.32.*
python-multipart==0.0.*
soundfile==0.12.*
torch==2.5.*
torchaudio==2.5.*
funasr==1.1.*
